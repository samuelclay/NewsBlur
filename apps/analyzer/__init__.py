"""Intelligence trainer and story classification.

Stores per-user classifiers (title, author, tag, feed, URL, text, and AI prompt)
that score stories as liked (+1), neutral (0), or disliked (-1). Classifiers can
be scoped to a single feed, a folder, or applied globally.
"""
