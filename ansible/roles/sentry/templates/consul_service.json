{
    "service": {
        "name": "{% if inventory_hostname == 'hdb-sentry' %}db-sentry{% else %}db-sentry-staging{% endif %}",
        "id": "{{ inventory_hostname }}",
        "tags": [
            "sentry"
        ],
        "port": 9000
    }
}
