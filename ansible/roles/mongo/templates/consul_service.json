{
    "service": {
        "name": "{{ inventory_hostname|regex_replace('\d+', '') }}",
        "id": "{{ inventory_hostname }}",
        "tags": [
            "db"
        ],
        "port": 27017,
        "checks": [{
            "id": "mongo-ping",
            "http": "http://127.0.0.1:5000/db_check/mongo",
            "interval": "15s"
        }]
    }
}  
