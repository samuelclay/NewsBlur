{
    "service": {
        "name": "{{ inventory_hostname|regex_replace('[0-9]+', '') }}",
        "id": "{{ inventory_hostname }}",
        "tags": [
            "db"
        ],
        "port": 27017
    }
}  
