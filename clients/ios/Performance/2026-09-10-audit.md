# iOS scrolling audit, September 10, 2026

Worktree: `ios-scroll-performance`. Runtime: NB Alpha, iPhone 17e simulator, iOS 26.5, the existing `samuel` account. Mark-as-read-on-scroll is enabled. The standard native story list retains its titles, previews, thumbnails, feed colors, metadata, intelligence indicators, related-story rows, and read-state dimming.

## Latest integrated measurements

The 228-test checkpoint passes the full Alpha target with zero failures in 65.361 seconds. Production source is `17e6af1c183805daa21edf00db1c88ecdcc41049`; the preserved `NB Alpha integrated 228-green.app` executable has SHA-256 `c3d504bfba7701ad6b4a62a353a8c4d299a1a84fd4e0709f23c99c7fae2456e5`. Evidence: `integrated-final-after.xcresult` and `/tmp/newsblur-scroll-integrated-final-tests.log`. Subsequent test-only commits cover successive searches and explicit status-bar scrolling to the top; those follow-ups are separate from this checkpoint.

The final feed comparison uses the first fast pass after a fresh launch, followed by a same-process warm repeat, with seed 417 and 24 gestures. Both binaries retain Objective-C `-O0` and Swift optimization. The preceding integrated build already contains the earlier fixes; this comparison isolates the remaining cold artwork preparation work more closely than an original-versus-final comparison would.

| Feed measurement | Before cold artwork preparation (`final-feeds-08`) | Prepared first pass (`after-feeds-final-10`) | Prepared warm repeat (`after-feeds-warm-final-10`) |
| --- | ---: | ---: | ---: |
| Cell configuration mean | 0.784 ms | 0.207 ms | 0.178 ms |
| Cell configuration p95 | 1.485 ms | 0.421 ms | 0.413 ms |
| Worst cell configuration | 42.100 ms | 9.930 ms | 1.169 ms |
| Drawing mean | 0.344 ms | 0.316 ms | 0.318 ms |
| Main-thread frame gaps | 12 / 1,249 | 3 / 1,254 | 1 / 1,259 |
| Excess frame-gap time | 350.578 ms | 50.360 ms | 17.167 ms |
| Worst main-thread frame gap | 96.447 ms | 33.667 ms | 33.834 ms |

Mean first-pass cell cost falls 74%, p95 falls 72%, and excess frame-gap time falls 86%. Cell counts differ as deceleration and asynchronous refresh affect the traversed rows. The final cold capture records all three font preparations succeeding at approximately 1,085 ms, with no failures, and a 3.773 ms populated feed-artwork snapshot. These are preparation measurements, not a claim that a network-dependent cold launch is instant. Videos and summaries are in the directories above, `after-cold-final-10`, and `final-feed-comparison-10.json`.

The 228-test checkpoint also completes thumbnail memory-warning races and decoded-byte accounting. A scale-three fixture previously charged 14,400 bytes for a bitmap requiring 132,480 bytes. The existing 20 MiB memory cache now receives the actual row-stride cost, while active/queued preparation is canceled on memory warnings. Source metadata survives bitmap-only eviction. The cache budget does not cap total process memory; an active decode can temporarily exist outside it.

The matched Dwell article scroll retains RSS mode, the same three search results, full typography/images, and 24 gestures with seed 1907. The preceding navigation build records 19 gaps in 1,343 intervals, 199.295 ms excess time, and a 38.289 ms maximum. The integrated build records 14 gaps in 1,330 intervals, 139.655 ms excess time, and a 28.911 ms maximum. Gradient callback means are 0.148 and 0.158 ms; this particular comparison does not establish a gradient-cost improvement. Evidence: `before-detail-scroll-11`, `after-detail-scroll-13`, and `detail-final-comparison-13.json`.

Article opening captures exposed an explicit scroll-to-top persistence edge case: a status-bar tap reaches the native negative-inset top, but reopening restores the previous saved position. The earlier after-opening captures are retained as `after-detail-saved-position-11` and `after-detail-status-top-repro-12`, not used as matched top-opening latency evidence. Likewise, `after-detail-bottom-scroll-11/12` begin at the bottom and are excluded from the matched scrolling comparison. The valid after scroll starts from the visually verified `after-detail-scroll-start-13.png`. The before opening's first sample also restores an older position; only its later openings begin at the top.

## First measured iteration

Both builds use Alpha Release with the existing Objective-C optimization setting (`-O0`). The baseline app was preserved before the production fixes. Measurements run with video recording, symbolized CPU sampling, and the opt-in display-link/cell probes described in [README.md](README.md).

| Measurement | Before | First fixes |
| --- | ---: | ---: |
| Feed cell configuration p95 | 1.324 ms | 1.218 ms |
| Feed drawing p95 | 1.230 ms | 1.192 ms |
| Folder header drawing p95 | 3.910 ms | 2.953 ms |
| Feed main-thread frame gaps | 7 / 1,262 | 4 / 1,269 |
| Story cell configuration p95 | 1.139 ms | 1.212 ms |
| Story scroll-to-read callback mean | 0.223 ms | 0.078 ms |
| Story height callbacks | 4,461 | 1,593 |
| Story main-thread frame gaps | 55 / 1,184 | 34 / 1,250 |
| Story worst main-thread frame gap | 99.964 ms | 35.732 ms |
| Story excess frame-gap time | 1,707.903 ms | 535.621 ms |
| Detail gradient updates | 5,843 | 1,332 |
| Detail main-thread frame gaps | 11 / 860 | 11 / 915 |
| Detail worst main-thread frame gap | 39.965 ms | 32.341 ms |

Feed and title runs use 24 gestures with seeds 417 and 812. Detail uses 16 gestures with seed 213 on the same TechRadar full-text article (`7564:efaf71`). The after detail article was located through search, so the neighboring articles differ. Live unread story lists also change as scrolling marks stories read and new stories arrive; these are production-shaped runs, not identical immutable datasets. The controlled regression benchmarks supplement the videos. A frame gap here means a main-thread callback interval above 1.5 times its requested interval, not a GPU presentation measurement.

The first iteration removes unnecessary SwiftUI story-model reconstruction from the native list, avoids reloading cells just to update scroll-read state, caches rendered custom icons, and avoids redundant detail toolbar/adjacent-page updates. The thumbnail path retains its existing bitmap during refresh and deduplicates image requests while retaining changed-source, empty-source, retry, and obsolete-response behavior.

All 38 first-iteration regressions passed. The 300-render custom-icon microbenchmark fell from approximately 84 ms to 0.94 ms. Pixel comparisons check cached custom icons against fresh drawing across icon types, sizes, and appearance changes.

The feed improvement was modest. The after CPU profile still sampled synchronous favicon disk reads during cell configuration. A read-only audit of the actual cached archives found 651 images totaling approximately 64 MB decoded, including 42 images at 384×384 pixels. Warming all originals into a 5 MB memory cache creates churn. The next feed experiment uses the existing rounded artwork at its displayed resolution, retaining originals for larger uses.

## Second comparison and targeted follow-ups

The second comparison retains the original build and uses the same gesture seeds, counts, native layout, preview settings, and mark-on-scroll preference. Both detail runs now open the same article through the same search results, so neighboring pages match as well.

| Measurement | Original, run 2 | Second fixes, run 2 |
| --- | ---: | ---: |
| Feed cell configuration mean | 0.543 ms | 0.440 ms |
| Feed cell configuration p95 | 1.211 ms | 1.501 ms |
| Feed drawing mean | 0.522 ms | 0.292 ms |
| Feed main-thread frame gaps | 5 / 1,288 | 8 / 1,266 |
| Story scroll-to-read callback mean | 0.213 ms | 0.083 ms |
| Story main-thread frame gaps | 54 / 1,192 | 14 / 1,260 |
| Story worst main-thread frame gap | 93.820 ms | 35.416 ms |
| Story excess frame-gap time | 1,343.084 ms | 227.250 ms |
| Detail gradient updates | 5,992 | 1,431 |
| Detail main-thread frame gaps | 13 / 915 | 10 / 920 |
| Detail excess frame-gap time | 135.759 ms | 118.540 ms |

Feed cell work decreased, but frame gaps did not consistently improve. One 100.662 ms gap included 82 cell configurations taking 71.280 ms, followed by 85 draws taking only 1.564 ms. The table hides descendant and duplicate parent rows with a zero height, while its cell construction omitted fewer cases. It was preparing full cells and loading icons for invisible rows. A regression reproduced 100 hidden rows causing 100 favicon reads; the third iteration returns the existing blank cell and skips prefetch for those rows. Positive-height artwork and search behavior remain covered.

Controlled tests also compare appending 12 stories at increasing list sizes:

| Existing stories | Original reload | Guarded insertion | Insertion with cached row heights |
| --- | ---: | ---: | ---: |
| 100 | 17.06 ms | 3.45 ms | 2.80 ms |
| 1,000 | 144.85 ms | 11.42 ms | 6.87 ms |
| 5,000 | 722.35 ms | 44.14 ms | 23.20 ms |

At 5,000 stories, preview normalization falls from 5,012 stories to 12. Tests check existing cell content, cluster mappings, scroll position, read-on-scroll anchor, filter/layout changes, empty and fully hidden pages, and full story-content replacement after sharing. The remaining 15.53 ms in this synthetic case is model traversal.

Read/unread model replacement at the end of a 5,000-story list falls from 4.270 to 0.767 ms per change, preserving collection snapshots and legacy identifier handling. Warm detail HTML preparation falls from 3.689 to 1.110 ms per page. Repeated bundled asset reads fall from 120 to zero over 12 draws across three controllers, with byte parity for all four themes, five scripts, embedded fonts, and changing dynamic content. The existing WebKit navigation/reveal sequence is unchanged in that iteration.

All 92 third-iteration tests pass. Additional thumbnail tests reproduce and fix removal-versus-promotion races, plus same-URL freshness after explicit refresh/account reset while retaining displayed images.

Cold-launch profiling is intrusive: CPU-sampled runs reached their first feed cell at 3.73–4.03 seconds from the launch request. Controls without CPU attachment reached it at 1.452 seconds originally and 1.469 seconds in the third build (probes ready at 0.746 seconds, view-controller preparation 316 ms). These changes do not establish a cold-start improvement. Treat the sampled startup numbers as diagnostic evidence, not ordinary launch performance. A recorded warm All Site Stories opening took 762 ms from the automation checkpoint to first cell configuration; this is not a full-image/web-content completion measurement.

## Repeated feed runs and longer story stress

Warm feed repeats use the second pass through the same process, with seed 417 and 24 gestures:

| Measurement | Original warm run 4 | Third fixes warm run 4 |
| --- | ---: | ---: |
| Feed cell configuration mean | 0.222 ms | 0.201 ms |
| Feed cell configuration p95 | 0.472 ms | 0.436 ms |
| Feed drawing mean | 0.482 ms | 0.347 ms |
| Feed main-thread frame gaps | 5 / 1,258 | 2 / 1,257 |
| Feed excess frame-gap time | 98.735 ms | 33.398 ms |

First-pass sampled run 3 was inconsistent: the original had five gaps with an 83.531 ms maximum; the third build had seven gaps with a 232.176 ms maximum. The latter contains a single 209.992 ms feed drawing event whose exact cause is unresolved. It is retained in the evidence, rather than excluded from the comparison.

A repeat with CPU sampling disabled (`*-feeds-no-sampler-05`) had mean cell configuration of 0.478 to 0.238 ms, mean feed drawing of 0.433 to 0.263 ms, five to four gaps, and maximum gaps of 82.775 to 40.458 ms. The 232 ms outlier did not recur. This control does not prove that sampling caused that particular outlier. Identical gestures can traverse different numbers of rows when deceleration and asynchronous feed refresh differ.

The third short title run had 25 gaps in 1,113 intervals and a 35.664 ms maximum. Its longer 100-gesture run (seed 9001) rendered 769 cells at 0.337 ms mean / 1.140 ms p95. It had 65 gaps in 5,029 intervals, a 124.920 ms maximum, and 47 full reload callbacks averaging 14.429 ms with a 38.020 ms maximum. The CPU profile identifies `fetchRiverPage:withCallback:` calling the initial `loadingFeed` presentation on every page. That schedules immediate and delayed full reloads through `changedLayout`, despite the guarded insertion in `renderStories:`. A separate regression and fix retain the presented native list while requesting a later page.

The worst long-run gap contains a 68.275 ms content drawing event. Aggregated CPU samples point to title measurement and first-use CoreText font fallback, but cannot attribute an individual timed event to a particular story. Component probes will distinguish title measurement, text drawing, and image drawing before choosing another rendering change.

Third detail run 3 retained a previously saved reading position, so its 12 gaps / 767 intervals are not a matched starting-position comparison. The separate `before-detail-navigation-04` recording reopens the same full-text article three times from the top, without CPU sampling, as the reference for the single-navigation experiment.

## River presentation fix and exact text-layout diagnosis

The next candidate skips the initial loading presentation for subsequent pages of an existing native river list. Sixteen pagination tests pass, including the new regression and non-native/daily-briefing preservation cases. The new regression fails against the preceding binary. The short run records no full reloads across 11 responses, down from 16 reloads in the preceding short run.

| 100 gestures, seed 9001 | Third fixes | River presentation fix |
| --- | ---: | ---: |
| Full story-list reloads | 47 | 0 |
| Story height callbacks | 17,807 | 6,572 |
| Cell configuration p95 | 1.140 ms | 1.074 ms |
| Page rendering callbacks | 24 | 23 |
| Main-thread frame gaps | 65 / 5,029 | 17 / 5,066 |
| Excess frame-gap time | 1,158.572 ms | 299.602 ms |
| Worst main-thread frame gap | 124.920 ms | 68.455 ms |

The live unread dataset changes between runs. The comparison preserves gesture seed/count, layout, previews, images, and mark-on-scroll behavior. `stress-titles-04` contains the later video, sample, component events, and summary.

At 49.2346 seconds into that long run, preview measurement takes 59.784 ms inside a 60.627 ms content draw, immediately followed by the 68.455 ms frame gap. The title measurement before it takes only 0.050 ms. The other measured drawing components total less than 1 ms. The symbolized CPU profile locates fallback-font loading and OpenType CID parsing inside the preview `boundingRect` call. This supports preparing the exact upcoming text layout before visibility, while retaining the existing typography and drawing.

The new short run (`after-titles-04`) has five gaps in 1,089 intervals, including a separate 324.698 ms outlier at 0.727 seconds. No long cell/drawing/layout/reload event overlaps it, and all five gaps occur within the first 1.84 seconds. The aggregate sample does not establish its cause. It remains in the report as an unexplained startup-of-scroll outlier.

First-page loading regressions are recorded in `fourth-firstpage-clean-before.xcresult`: ten tests produce ten expected assertion failures with no unexpected failures. The preceding mixed run (`fourth-before.xcresult`) also includes three fixture crashes from dereferencing an absent array; those fixtures were repaired before the clean reproduction. Article navigation regressions separately produce 13 expected assertion failures across 11 tests in the original two-navigation implementation. The preliminary single-navigation candidate passes the navigation identity and stale scroll-restoration checks; modal-return and actual WebKit resource-stall validation remain in progress.

## Cached opening and text preparation checkpoints

The cached-first candidate stores bounded, account/host/request-specific first-page responses and presents them before the existing queued read/save synchronization and refresh. Provisional read/save/tag state is overlaid from a bounded local edit journal. Server reconciliation preserves the visible row and pixel offset, and keeps an already open article available even when it disappears from the refreshed unread page. These are implementation checkpoints, not yet a measured end-to-end opening improvement.

The shared follow-up run (`shared-followup-before.xcresult`, `/tmp/newsblur-scroll-shared-followup-tests.log`) executes 72 selected tests with five assertion failures and no unexpected failures. All 21 cached-list integration tests pass. Seventeen of 18 storage tests pass; the new failing case distinguishes an explicit JSON null from a removed tag field across relaunch. Two text-layout regressions fail against the unchanged drawing implementation: the native table has no text-layout prefetcher, and a reused story hash with replacement title/preview text retains stale geometry. The 600-case original-layout pixel/size comparison and separate cluster drawing comparison both pass before introducing the text-preparation implementation.

The remaining two assertions concern a real WebKit fixture with an image deliberately held pending. Its complete article DOM has a 6,549-point body, but the native scroll view still reports the preceding blank page's 844-point height. A window attachment is blank, and releasing the image resets a manually assigned offset. Navigation-generation, modal-return, and completed-gesture restoration guards pass. Comparing the same fixture against the earlier navigation implementation and a minimal WebKit document is required before treating the pending-image result as a production regression.

## Warm opening measurements and appearance checks

The first cached-opening implementation passes 40 focused tests: 22 loading/reconciliation cases and 18 storage cases. `808e197ec` is the production checkpoint. Its saved runtime binary retains the preceding article-navigation implementation and does not yet include background text measurement, keeping this comparison specific to first-page loading.

Three openings each were recorded for All Site Stories and Engadget with CPU sampling disabled. The table uses openings two and three, after each request has a cached response. Times run from the automation tap checkpoint to completion of the first actual story-content draw, including automation overhead:

| Warm opening | Previous implementation | Cached first page |
| --- | ---: | ---: |
| All Site Stories, second opening | 1,148 ms | 224 ms |
| All Site Stories, third opening | 934 ms | 231 ms |
| Engadget, second opening | 671 ms | 211 ms |
| Engadget, third opening | 695 ms | 204 ms |

The recordings show cached stories already present during the push transition. The preceding build displays its loading bar before the story rows arrive. Fresh responses still reconcile afterward: 864–930 ms for the two warm river openings and 548–608 ms for the feed. These measurements do not establish a cold cache-miss improvement; the first after river opening took 2,021 ms in this run. First openings can also contain residual draws from an outgoing view, so they are excluded from the table. `cell.story` includes loading-bar cells and must not be used as a content-readiness metric. The content-draw metric does not mean every remote image has finished loading.

`cached-opening-comparison.json` contains all three openings and both cell/content timings. Videos, screenshots, and inspected transition contact sheets are in `before/after-river-opening-05` and `before/after-feed-opening-05`.

The same 40-test binary was manually checked in Light, Sepia, Medium, and Dark across the feed list, native story list, and article detail. Feed artwork/counts, title and preview typography, image placement, metadata, read-state dimming, article body, and toolbar visuals are retained. The same Engadget Avatar article was opened through the All filter for each theme. Auto theme and Unread filtering were restored afterward. Screenshots are in `theme-ui-05`; the valid Sepia article and All-list captures have the `-ready` suffix. The pending article-navigation and text-prefetch changes still require an integrated runtime check.

## Exact text preparation on the live story list

`d255e29c4` adds bounded background measurement of the exact upcoming title and normalized preview, retaining the existing fonts, paragraph settings, truncation, geometry, and drawing. The cache has synchronous fallback when a row becomes visible before preparation completes. Thirteen focused tests pass across two shared runs, including cancellation and 1,080 prepared pixel/size comparisons across the four themes, widths, text sizes, image positions, and cell states. Prepared first draws perform no main-thread `boundingRect` calls. The cold multilingual fixture takes 89.018 ms on the worker; its separate 61.165 ms cell/bitmap-render/extraction interval does not identify an individual drawing cost.

The next live title capture uses the same 24-gesture warmup followed by 100 gestures with seed 9001. The compiled app includes this text change, the 40-test first-page checkpoint, and the preliminary article-navigation experiment; these runs remain entirely in the native story list.

| 100 gestures, seed 9001 | River presentation fix | Exact text preparation |
| --- | ---: | ---: |
| Cell configuration p95 | 1.074 ms | 0.852 ms |
| Story content draw mean | 0.874 ms | 0.663 ms |
| Story content draw p95 | 1.467 ms | 1.254 ms |
| Worst story content draw | 60.627 ms | 3.443 ms |
| Main-thread frame gaps | 17 / 5,066 | 7 / 5,179 |
| Excess frame-gap time | 299.602 ms | 90.477 ms |
| Worst main-thread frame gap | 68.455 ms | 34.243 ms |
| Full story-list reloads | 0 | 0 |

The prepared run has only 24 main-thread title/preview measurements across 1,052 content draws; the regular preview drawing probe fires 797 times. Cluster rows retain their original path. Maximum main preview measurement is 0.286 ms, title measurement 0.105 ms, and preview drawing 0.712 ms. The previously identified 59.784 ms preview-layout stall does not recur. Live stories differ between runs as normal scrolling consumes unread items.

The sampled short run still has an unexplained early 352.285 ms gap, with 21 gaps in 1,209 intervals. Its largest measured content draw is 12.660 ms, including 11.319 ms title drawing. A fresh-process repeat without CPU sampling has two gaps in 1,260 intervals, 33.220 ms excess time, a 33.313 ms maximum gap, and a 2.529 ms maximum content draw. The large gap did not recur in that control; this alone does not prove the sampler caused it. The sampled profile includes deferred Core Animation image decoding outside the cell's drawing callback, but aggregate stacks cannot attribute it to this timed outlier.

Evidence and summaries are in `after-titles-05`, `stress-titles-05`, and `after-titles-no-sampler-06`. The end screenshots retain full previews, thumbnails, related-story rows, and read-state dimming. Reverse scrolling after preview-cache eviction remains an additional preparation case under test.

## Correctness follow-ups and rejected article experiment

The completed first-page implementation passes 48 cases: 28 loading/reconciliation cases and 20 storage cases. Follow-up commits `a6d2b782a` and `12c22c078` preserve failed queued read/save/tag edits, discard incomplete persisted history after an unclean session, reject malformed cached metadata, retain pagination request gates, and keep an open article through far-index swipes, rotation, and layout refresh. A same-run 5,000-story full read/unread benchmark measures 1.477 ms per replacement without the journal and 1.493 ms with it. Isolated journal updates average 2.53 microseconds; a 500 KB response enqueue takes 0.0072 ms and the foreground durability marker 0.429 ms. No synchronous disk write is added to each scroll edit. Evidence: `https-origin-control.xcresult` and `/tmp/newsblur-scroll-https-control-tests.log`.

The title videos expose another loading delay: the original pagination threshold is only 500 points from the bottom. A reproducing test places the view 1,000 points from the bottom and confirms no next-page request starts. `90460438d` uses a bounded reserve of three viewport heights for the native list, retaining one request at a time and the existing daily-briefing path. All 18 pagination tests pass. A separate 1,000-story eviction regression confirms an older row with an evicted preview falls back to HTML normalization and both text measurements on main. Its bounded worker candidate changes those counts from one main normalization/two main measurements to one worker normalization/zero main measurements; cancellation and stale-content tests are being completed.

The immediate complete-document article experiment is **not accepted as a finished fix**. The corrected fixture uses the production HTTPS base URL and a loopback HTTP server with a real bound port, sends image headers, and deliberately holds the image body. The preceding two-stage implementation renders a native 6,568-point article and preserves manual scrolling while the image remains pending. The single-navigation candidate has a complete DOM and loaded Gotham font but retains the 763-point blank viewport; finishing the image resets a manually assigned reading offset. This is a real regression (`http-before-navigation.xcresult` versus `http-ready-control.xcresult`). Earlier loopback attempts accidentally used port zero and are not evidence of HTTP/mixed-content rejection.

An independent HTML bisect points to the embedded font declarations: removing them allows incremental rendering, while removing scripts or the DOM-ready notification does not. Fonts and final article visuals remain unchanged. A bounded experiment will warm the same fonts in the existing hidden bootstrap document; if that does not preserve loading behavior and improve normal latency, the guarded two-stage sequence will remain. Immediate database scroll restoration also races native WebKit layout in both implementations and requires a separate fix.

## Eviction and shared thumbnail correctness

`df1f74f27` completes the preview-eviction follow-up. All five regressions pass: preparing an evicted older preview, cancellation before work begins, replacing content under the same story hash during work, resetting the render generation during an active batch, and bounding retained raw content to 4 MiB. The worker retains at most 24 nearby rows and validates the current path, generation, hash, and content before publishing. The 1,000-story fixture changes one main-thread HTML normalization and two main-thread measurements into one worker normalization and zero main-thread measurements. Evidence: `paint-and-preview-checkpoint.xcresult`.

Independent review also reproduced overlapping title controllers publishing thumbnails into the shared image cache. An older URL request could replace the newer bitmap after the newer controller had recorded its source as complete. `336b753f1` gives shared requests an identity and checks it atomically when publishing the bitmap and its source metadata. Identical concurrent sources can share ownership; source changes, explicit refreshes, removal, and account reset invalidate older ownership. Rejected completions clear their controller's pending request so later requests can retry. Existing visible images remain available during refresh. All 34 thumbnail cases pass in `detail-failure-before.xcresult`; that selected run also contains intentional article-loading failures and is not a full-suite green result.

## Integrated checkpoint and deeper cold-path audit

The integrated checkpoint `f73ccf653` passes all 203 Alpha tests (`integrated-all-after.xcresult`, 57.3 seconds of test execution). The preserved app is `NB Alpha integrated 203-green.app`, executable SHA-256 `6dd3fc0c7c51c0117494a973c0c92ee582411e34722d044f6337b273fb145b4a`. This is a regression checkpoint, not completion of the runtime audit.

The article candidate warms the three existing embedded fonts in the hidden bootstrap document, then submits one complete article document. It retains the existing 100 ms reveal delay and falls back to the guarded preceding navigation sequence if preparation fails. Real WebKit tests now cover first and second articles with an image body held pending, delayed native geometry, saved-position restoration, finger and keyboard movement, modal return, obsolete navigations, and process replacement. The held-image fixture renders a 6,568-point native article; its 16–33 ms first-contentful-paint measurements begin at complete-document navigation, excluding initial bootstrap, tap dispatch, and reveal delay.

The actual cold launch exposes a missing lifecycle case: all three font preparations time out at approximately 1,019 ms and use the fallback sequence. Therefore the normal app has not yet established the candidate's article-opening benefit. First feed-cell timing without CPU sampling is 1,526 ms for the preceding navigation build and 1,485 ms for the candidate, with view preparation of 339 and 338 ms respectively. This small difference does not establish a cold-start improvement. Evidence: `before/after-cold-navigation-07`. A new detached-container test is being compared with the real launch before changing the timeout or readiness condition.

The preceding-navigation binary records three matched openings and a 24-gesture scroll of the same Dwell Saltburn article in RSS mode. `before-detail-navigation-07/transition-contact.png` shows the full article body and estate image appearing during a push transition; the three opening screenshots retain the same top position. `before-detail-scroll-07/contact.png` checks the article from title to end, including images, links, text, and action controls. Its scroll reference has 16 gaps in 1,332 intervals, 168.348 ms excess time, and a 30.653 ms maximum gap. The earlier TechRadar article no longer appears in the same search, so the Dwell recordings form a new comparison pair rather than being compared against TechRadar timings.

The feed first pass still differs markedly from its warm repeat, both using 24 gestures with seed 417:

| Integrated feed run | First pass | Same-process warm repeat |
| --- | ---: | ---: |
| Cell configuration mean | 0.784 ms | 0.188 ms |
| Cell configuration p95 | 1.485 ms | 0.430 ms |
| Worst cell configuration | 42.100 ms | 1.644 ms |
| Drawing mean | 0.344 ms | 0.345 ms |
| Main-thread frame gaps | 12 / 1,249 | 4 / 1,266 |
| Excess frame-gap time | 350.578 ms | 68.140 ms |
| Worst main-thread frame gap | 96.447 ms | 33.981 ms |

`final-feeds-08/cpu.txt` identifies synchronous favicon disk-cache locking in visible cell construction and UIKit cell prefetch. Preparing only imminent rows can begin too late on the first pass. The follow-up regression uses 651 feed rows and real encoded image files; its candidate will prepare bounded display-sized artwork in displayed order before those rows become visible. The rejected whole-original-image warmup remains excluded. The mistakenly named earlier `final-feeds-07` capture actually shows the All story list and has been renamed `final-titles-all-07`; it is not feed evidence.

The new integrated title run retains Unread, Newest, List, two preview lines, large right-side images, medium text, Comfortable spacing, Auto theme, and “On scroll or selection” read behavior. The checked preference screenshot is `mark-scroll-verified-07.png`. No mark-all action was used.

| Integrated title run | 24 gestures, seed 812 | 100 gestures, seed 9001 |
| --- | ---: | ---: |
| Cell configuration mean | 0.337 ms | 0.365 ms |
| Cell configuration p95 | 1.179 ms | 1.189 ms |
| Content drawing mean | 0.825 ms | 0.772 ms |
| Content drawing p95 | 1.706 ms | 1.461 ms |
| Worst content draw | 2.792 ms | 6.657 ms |
| Main-thread frame gaps | 13 / 1,193 | 16 / 5,190 |
| Excess frame-gap time | 198.556 ms | 242.015 ms |
| Worst main-thread frame gap | 35.684 ms | 33.680 ms |
| Full list reloads | 8 during initial reconciliation | 0 |

The 100-gesture run performs no main-thread title/preview measurements. A further 200 gestures with seed 12200 reach 1,029 loaded stories and 717 visible rows, exceeding the 512-story preview cache. Its 2,200 content draws average 0.721 ms, with 1.332 ms p95 and a 19.511 ms maximum; only five main-thread title/preview measurements remain, all below 0.4 ms. The run has 58 gaps in 9,460 intervals and a 36.765 ms maximum.

The reverse recording then traverses the older rows with 90 downward gestures. Video inspection shows that it reaches the top around 18 seconds and spends the remaining gestures rubber-banding there. The separate first-15-second summary isolates actual traversal: 785 cell configurations, 958 content draws averaging 1.168 ms, 2.223 ms p95, and a 14.006 ms maximum. All 57 frame gaps occur in those first 829 intervals, with 932.356 ms excess time and a 40.609 ms maximum. Counting the later top bounces would misleadingly dilute that gap rate. Main preview and title measurements remain below 0.400 and 0.475 ms respectively.

The reverse CPU profile identifies another disk path: 155 of 517 content-drawing samples call `cachedImageForStoryHash:` through `PINDiskCache` to read and decode archived thumbnails. A bounded nearby-thumbnail preparation regression is the next follow-up. The screenshot and contact sheet retain original thumbnails, preview text, feed artwork, read-state dimming, and related-story rows. Evidence: `deep-forward-titles-08`, `deep-reverse-titles-08`, `deep-and-detail-comparison-08.json`, and `deep-reverse-titles-08/traversal-summary.json`.

## Cold-path fixes and guarded thumbnail preparation

The detached WebKit regression succeeds with the previous timer, so being off-window is not the cause of the real cold failure. The diagnostic app instead shows all three navigation starts at 1,106–1,109 ms after bootstrap submission and finishes at 1,119–1,120 ms; the idle timer already failed them at 1,059–1,060 ms. `a650b042b` starts the one-second deadline only when a complete article is waiting, with a separate generation for each reused page. Idle preparation can finish normally. The real cold launch in `after-cold-font-ready-09` now records all three successful preparations at 1,129 ms, with zero failures. The first feed cell appears 1,431 ms after launch; this remains within the preceding cold-launch range rather than establishing a substantial startup improvement.

The combined focused checkpoint passes 103 tests: 33 detail, 41 thumbnail, and 29 feed/icon/hidden-row cases. Its saved app is `NB Alpha cold fixes103-green.app`; `cold-fixes-focused-after.xcresult` and `/tmp/newsblur-scroll-cold-fixes-focused-tests.log` contain the result. The full-target run and final live recordings remain separate validation steps.

`194d9a137` prepares feed artwork in displayed order using the existing rounded pixels, a serial worker, and an actual decoded-cost admission budget of 8 MiB. The original-image cache receives no proactive promotions. Collapsed, duplicate, filtered, saved-feed, and custom-icon rows keep their existing behavior. Memory warnings and account/source/plan changes cancel stale preparation. When the proactive budget is exhausted, normal nearby-row prefetch and demand loading remain available.

| 651-feed fixture | Original | Prepared artwork |
| --- | ---: | ---: |
| Foreground disk reads | 651 | 0 |
| Worker disk reads | 0 | 651 |
| Cell configuration mean | 0.879 ms | 0.115 ms |
| Cell configuration p95 | 1.024 ms | 0.119 ms |
| Worst cell configuration | 2.737 ms | 4.720 ms |

The snapshot plus table reload takes 5.938 ms in the fixture; the real cold app's populated preparation snapshot takes approximately 4.1 ms. The isolated worst cell is higher in the candidate, so the supported improvements are mean/p95 cost and removal of foreground I/O. Live frame recordings decide whether those gains improve scrolling. Formal before/after logs are `newsblur-scroll-hidden-bootstrap-red-tests.log` and `newsblur-scroll-cold-fixes-focused-tests.log` in `/tmp`. The intermediate `idle-bootstrap-red-tests` log contains a rejected request-builder implementation that accidentally scheduled no worker reads; it is not the original baseline.

The thumbnail reverse fixture enters native table prefetch and then draws an evicted row from a 1,000-story collection. Foreground disk reads change from one to zero and worker reads from zero to one, with byte-identical rendered cell PNGs. The candidate retains the synchronous fallback and the existing bounded image memory cache. Its printed drawing interval includes PNG serialization, so that fixture is used for thread ownership and pixel parity, not as a real cell drawing-time claim. Final follow-ups cover cancellation of active and queued work on memory warning and decoded-byte accounting for images whose scale exceeds one.

## Local evidence

Artifacts are in `/tmp/newsblur-scroll-evidence` on the development Mac:

| View | Before | First fixes |
| --- | --- | --- |
| Feeds | `before-feeds-01/scroll.mp4` | `after-feeds-01/scroll.mp4` |
| Story titles | `before-titles-01/scroll.mp4` | `after-titles-01/scroll.mp4` |
| Story detail | `before-detail-01/scroll.mp4` | `after-detail-01/scroll.mp4` |

Each directory also contains `cpu.txt`, `measurements.jsonl`, and `session.json`. The full first-iteration test result is `combined-after.xcresult`; the original regression failures are in `regression-before.xcresult`, with clean detail failures separately in `detail-clean-before.xcresult`.

Second comparison directories use `before-*-02` and `after-*-02`; `second-comparison.json` contains their summaries. Third iteration regressions are `third-before.xcresult` and `third-after.xcresult`. The startup control is `after-cold-no-sampler-03`, recorded with `IOS_CAPTURE_CPU=0`.

Later summaries are `third-feeds-comparison.json`, `feeds-no-sampler-comparison.json`, `third-titles-comparison.json`, and `stress-titles-03/summary.json`. The repeated feed videos use `before/after-feeds-warm-04` and `before/after-feeds-no-sampler-05`; the long title video is `stress-titles-03/scroll.mp4`.
